To test the functionality of the Project, please run the project in this order:
Database_Population.sql (Schema + Triggers) -> database_population.sql (Populate the database) -> project_queries.sql -> trigger_tests -> access_control.sql
